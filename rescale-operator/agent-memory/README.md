# Agent Memory & RAG

**Status:** Shipped — enable `NEXT_PUBLIC_AGENT_MEMORY` for dedicated UI capture.

Read-only agent query over historical run data (Supermemory), structured proposals, HITL promote path behind separate flag.

Copy structure from `../rescale-operator/index.html`.
